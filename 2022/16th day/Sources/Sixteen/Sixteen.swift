@main
public struct Sixteen {

    struct Pipe {
        let name: String
        let flowRate: Int
        var adjacent: [String]
    }

    public static func main() {
        
    }

    func parseInput(input: String) -> [Pipe] {
        let inputArray: [String] = input.
    }
}
